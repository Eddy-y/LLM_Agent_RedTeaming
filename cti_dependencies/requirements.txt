# Ultra-minimal Lambda worker dependencies
# boto3/botocore are provided by Lambda runtime - excluded

# Core dependencies (Lambda worker only)
neo4j>=5.14.0
psycopg2-binary>=2.9.9
pydantic>=2.7.4

# Note: langchain/langgraph are NOT needed in Lambda
# They are only used by graph_agents.py which runs locally
